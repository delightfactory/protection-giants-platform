name: Temp Verify Public Center UI

on:
  push:
    branches:
      - feature/public-center-directory-map
    paths:
      - .github/workflows/temp-verify-public-center-ui.yml

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
